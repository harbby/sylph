language: java
#jdk:
#- openjdk8
sudo: required


services:
- docker

matrix:
  include:
    - script: "gradle clean checkstyle licenseMain licenseTest --no-daemon"
      jdk: openjdk8
      dist: xenial
    - script: "gradle clean assemble -Pjdk=java8 --no-daemon"
      jdk: openjdk8
      dist: xenial
    - script: "./gradlew clean assemble -Pjdk=java11 --no-daemon"
      jdk: openjdk11
      dist: focal

env:
  - CI=false

before_deploy:
- echo build start... sylph
before_cache:
- rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
- rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/wrapper/"
